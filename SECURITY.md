# Security

اگر باگ امنیتی پیدا کردی، لطفاً قبل از انتشار عمومی جزئیاتش یک Issue عمومی نساز.

برای Deploy عمومی:

- فایل `.env`، کلیدها و Runtime data را Commit نکن.
- Node را مستقیم روی اینترنت Bind نکن؛ پیش‌فرض پروژه `127.0.0.1` است.
- پروژه را پشت Reverse Proxy اجرا کن و فقط پورت‌های وب لازم را باز نگه دار.
- Dependency alertهای GitHub/Dependabot را بررسی کن.

این پروژه جای WAF یا Rate Limit لایه Reverse Proxy را نمی‌گیرد؛ محدودیت‌های داخل برنامه یک لایه محافظ اضافه هستند.


## Private rooms / E2EE

وقتی میزبان برای Room رمز می‌گذارد، محتوای جدید Chat (متن، تصویر و ویس) قبل از ارسال در مرورگر با AES-256-GCM رمز می‌شود. کلید محتوای اتاق در Fragment لینک خصوصی (`#k=...`) و Local Storage همان مرورگر نگه داشته می‌شود؛ Fragment در درخواست HTTP به Server ارسال نمی‌شود. Server فقط ciphertext و metadata عملیاتی لازم مثل زمان/شناسه پیام را می‌بیند. هر بار که یک Room عمومی تازه Private می‌شود، یک کلید محتوای تازه ساخته می‌شود و با غیرفعال‌کردن Private Mode کلید ذخیره‌شده همان Room از مرورگر حذف می‌شود.

محدودیت مهم: این مدل در برابر Server/Storage/Hosting که فقط داده عبوری یا فایل‌های ذخیره‌شده را می‌بیند از محتوای Chat محافظت می‌کند. اگر اپراتور Server بتواند JavaScript برنامه را عمداً تغییر دهد، چون Web Client از همان Server لود می‌شود می‌تواند کدی برای سرقت کلید به Client تحویل دهد. برای مقاومت در برابر این سناریو به Client مستقل یا Build قابل‌تأیید/امضاشده نیاز است.


### Media در اتاق خصوصی

- نام و MIME واقعی فایل خصوصی داخل payload رمزنگاری‌شده قرار می‌گیرد؛ فایل ذخیره‌شده روی Server فقط `application/octet-stream` و ciphertext است.
- فایل خصوصی با permission محدود ساخته می‌شود و پاسخ HTTP آن `no-store`، `nosniff`، `same-origin` و `noindex` دارد.
- URL فایل به‌تنهایی برای مشاهده محتوا کافی نیست؛ بدون کلید AES همان Room فقط ciphertext دریافت می‌شود.
- Metadata عملیاتی مثل شناسه پیام، فرستنده، زمان و اندازه تقریبی همچنان برای مدیریت Room روی Server قابل مشاهده است؛ E2EE محتوای پیام/تصویر/ویس را پنهان می‌کند، نه تمام Metadata را.
