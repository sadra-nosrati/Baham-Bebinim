# Security

اگر باگ امنیتی پیدا کردی، لطفاً قبل از انتشار عمومی جزئیاتش یک Issue عمومی نساز.

برای Deploy عمومی:

- فایل `.env`، کلیدها و Runtime data را Commit نکن.
- Node را مستقیم روی اینترنت Bind نکن؛ پیش‌فرض پروژه `127.0.0.1` است.
- پروژه را پشت Reverse Proxy اجرا کن و فقط پورت‌های وب لازم را باز نگه دار.
- Dependency alertهای GitHub/Dependabot را بررسی کن.

این پروژه جای WAF یا Rate Limit لایه Reverse Proxy را نمی‌گیرد؛ محدودیت‌های داخل برنامه یک لایه محافظ اضافه هستند.
